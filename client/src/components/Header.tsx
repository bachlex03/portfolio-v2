import clsx from "clsx";
import styles from "./styles.module.css";
import { calibreFont, spaceMono } from "~/fonts/font.config";

export default function Header() {
  return <header className="py-8">header</header>;
}
